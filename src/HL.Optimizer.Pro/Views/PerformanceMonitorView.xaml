<UserControl x:Class="HL.Optimizer.Pro.Views.PerformanceMonitorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1000">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="MONITOR DE DESEMPENHO" Foreground="White" FontSize="22" FontWeight="Bold"/>
            <TextBlock Text="Monitor em tempo real - CPU, RAM, GPU, Disco, Rede - atualização configurável" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,4,0,0"/>
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource CardBorder}" Margin="0,16,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal">
                    <Border Background="{StaticResource SurfaceDarkBrush}" CornerRadius="{StaticResource MediumCorner}" Padding="12,8" Margin="0,0,8,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="CPU" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding Current.CpuUsage, StringFormat={}{0:0}%}" Foreground="White" FontSize="14" FontWeight="Bold" Margin="8,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                    <Border Background="{StaticResource SurfaceDarkBrush}" CornerRadius="{StaticResource MediumCorner}" Padding="12,8" Margin="0,0,8,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="RAM" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding Current.RamUsage, StringFormat={}{0:0}%}" Foreground="White" FontSize="14" FontWeight="Bold" Margin="8,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                    <Border Background="{StaticResource SurfaceDarkBrush}" CornerRadius="{StaticResource MediumCorner}" Padding="12,8" Margin="0,0,8,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="GPU" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding Current.GpuUsage, StringFormat={}{0:0}%}" Foreground="White" FontSize="14" FontWeight="Bold" Margin="8,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                    <Border Background="{StaticResource SurfaceDarkBrush}" CornerRadius="{StaticResource MediumCorner}" Padding="12,8">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="DISCO" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding Current.DiskUsage, StringFormat={}{0:0}%}" Foreground="White" FontSize="14" FontWeight="Bold" Margin="8,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <TextBlock Text="Intervalo:" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <Button Content="1s" Style="{StaticResource SecondaryButton}" Padding="8,4" Margin="0,0,4,0" Command="{Binding SetIntervalCommand}" CommandParameter="1000"/>
                    <Button Content="2s" Style="{StaticResource SecondaryButton}" Padding="8,4" Margin="0,0,4,0" Command="{Binding SetIntervalCommand}" CommandParameter="2000"/>
                    <Button Content="5s" Style="{StaticResource SecondaryButton}" Padding="8,4" Margin="0,0,4,0" Command="{Binding SetIntervalCommand}" CommandParameter="5000"/>
                    <Button Content="10s" Style="{StaticResource SecondaryButton}" Padding="8,4" Command="{Binding SetIntervalCommand}" CommandParameter="10000"/>
                    <Button Content="Limpar" Style="{StaticResource SecondaryButton}" Margin="12,0,0,0" Command="{Binding ClearHistoryCommand}"/>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="2" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- CPU Chart Placeholder -->
            <Border Style="{StaticResource CardBorder}" Margin="0,0,8,8">
                <StackPanel>
                    <TextBlock Text="CPU - Histórico" Foreground="{StaticResource TextTertiaryBrush}" FontSize="10" FontWeight="Bold" LetterSpacing="1"/>
                    <Border Background="{StaticResource SurfaceDarkBrush}" CornerRadius="{StaticResource MediumCorner}" Height="160" Margin="0,12,0,0" Padding="12">
                        <Grid>
                            <TextBlock Text="{Binding Current.CpuUsage, StringFormat={}Uso atual: {0:0.0}%}" Foreground="White" FontSize="12" VerticalAlignment="Top"/>
                            <ItemsControl ItemsSource="{Binding History.Metrics}" VerticalAlignment="Bottom" Height="100">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Width="4" Background="{StaticResource ElectricBlueBrush}" Margin="1,0,0,0" CornerRadius="2" Height="{Binding CpuUsage}" VerticalAlignment="Bottom"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Grid>
                    </Border>
                    <Grid Margin="0,12,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Processos" Foreground="{StaticResource TextSecondaryBrush}" FontSize="10"/>
                            <TextBlock Text="{Binding Current.ProcessCount}" Foreground="White" FontSize="12" FontWeight="Bold"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Threads" Foreground="{StaticResource TextSecondaryBrush}" FontSize="10"/>
                            <TextBlock Text="{Binding Current.ThreadCount}" Foreground="White" FontSize="12" FontWeight="Bold"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource CardBorder}" Margin="8,0,0,8">
                <StackPanel>
                    <TextBlock Text="RAM - Histórico" Foreground="{StaticResource TextTertiaryBrush}" FontSize="10" FontWeight="Bold" LetterSpacing="1"/>
                    <Border Background="{StaticResource SurfaceDarkBrush}" CornerRadius="{StaticResource MediumCorner}" Height="160" Margin="0,12,0,0" Padding="12">
                        <Grid>
                            <TextBlock Text="{Binding Current.RamUsage, StringFormat={}Uso atual: {0:0.0}%}" Foreground="White" FontSize="12" VerticalAlignment="Top"/>
                            <ItemsControl ItemsSource="{Binding History.Metrics}" VerticalAlignment="Bottom" Height="100">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Width="4" Background="{StaticResource AccentGreenBrush}" Margin="1,0,0,0" CornerRadius="2" Height="{Binding RamUsage}" VerticalAlignment="Bottom"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Grid>
                    </Border>
                    <Grid Margin="0,12,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Usada" Foreground="{StaticResource TextSecondaryBrush}" FontSize="10"/>
                            <TextBlock Text="{Binding Current.RamUsedBytes}" Foreground="White" FontSize="11"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Disponível" Foreground="{StaticResource TextSecondaryBrush}" FontSize="10"/>
                            <TextBlock Text="{Binding Current.RamAvailableBytes}" Foreground="{StaticResource AccentGreenBrush}" FontSize="11"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>

            <Border Grid.Row="1" Style="{StaticResource CardBorder}" Margin="0,8,8,0">
                <StackPanel>
                    <TextBlock Text="DISCO - Leitura/Escrita" Foreground="{StaticResource TextTertiaryBrush}" FontSize="10" FontWeight="Bold" LetterSpacing="1"/>
                    <Border Background="{StaticResource SurfaceDarkBrush}" CornerRadius="{StaticResource MediumCorner}" Height="160" Margin="0,12,0,0" Padding="12">
                        <StackPanel>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Text="Leitura" Foreground="{StaticResource TextSecondaryBrush}" FontSize="10"/>
                                    <TextBlock Text="{Binding Current.DiskReadMBps, StringFormat={}{0:0.00} MB/s}" Foreground="White" FontSize="12" FontWeight="Bold"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="Escrita" Foreground="{StaticResource TextSecondaryBrush}" FontSize="10"/>
                                    <TextBlock Text="{Binding Current.DiskWriteMBps, StringFormat={}{0:0.00} MB/s}" Foreground="White" FontSize="12" FontWeight="Bold"/>
                                </StackPanel>
                            </Grid>
                            <ProgressBar Style="{StaticResource ModernProgressBar}" Value="{Binding Current.DiskUsage}" Margin="0,16,0,0" Foreground="#A78BFA"/>
                            <TextBlock Text="{Binding Current.DiskUsage, StringFormat={}Uso do disco: {0:0}%}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="10" Margin="0,8,0,0"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>

            <Border Grid.Row="1" Grid.Column="1" Style="{StaticResource CardBorder}" Margin="8,8,0,0">
                <StackPanel>
                    <TextBlock Text="REDE - Download/Upload" Foreground="{StaticResource TextTertiaryBrush}" FontSize="10" FontWeight="Bold" LetterSpacing="1"/>
                    <Border Background="{StaticResource SurfaceDarkBrush}" CornerRadius="{StaticResource MediumCorner}" Height="160" Margin="0,12,0,0" Padding="12">
                        <StackPanel>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Text="Download" Foreground="{StaticResource TextSecondaryBrush}" FontSize="10"/>
                                    <TextBlock Text="{Binding Current.NetworkDownloadMbps, StringFormat={}{0:0.00} Mbps}" Foreground="White" FontSize="12" FontWeight="Bold"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="Upload" Foreground="{StaticResource TextSecondaryBrush}" FontSize="10"/>
                                    <TextBlock Text="{Binding Current.NetworkUploadMbps, StringFormat={}{0:0.00} Mbps}" Foreground="White" FontSize="12" FontWeight="Bold"/>
                                </StackPanel>
                            </Grid>
                            <TextBlock Text="Monitoramento de tráfego de rede em tempo real" Foreground="{StaticResource TextTertiaryBrush}" FontSize="10" Margin="0,16,0,0"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
