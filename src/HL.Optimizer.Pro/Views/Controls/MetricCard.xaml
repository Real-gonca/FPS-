<UserControl x:Class="HL.Optimizer.Pro.Views.Controls.MetricCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="{StaticResource SurfaceDarkBrush}" CornerRadius="{StaticResource MediumCorner}" Padding="16">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <StackPanel Orientation="Horizontal">
                <Border Width="32" Height="32" CornerRadius="8" Background="{StaticResource SecondaryDarkBrush}">
                    <TextBlock Text="{Binding Icon, RelativeSource={RelativeSource AncestorType=UserControl}}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <TextBlock Text="{Binding Title, RelativeSource={RelativeSource AncestorType=UserControl}}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" Margin="8,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>
            <TextBlock Grid.Row="1" Text="{Binding Value, RelativeSource={RelativeSource AncestorType=UserControl}}" Foreground="White" FontSize="22" FontWeight="Bold" Margin="0,8,0,0"/>
            <ProgressBar Grid.Row="2" Style="{StaticResource ModernProgressBar}" Value="{Binding Percentage, RelativeSource={RelativeSource AncestorType=UserControl}}" Margin="0,8,0,0"/>
        </Grid>
    </Border>
</UserControl>
