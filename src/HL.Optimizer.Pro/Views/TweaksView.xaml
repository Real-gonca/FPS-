<UserControl x:Class="HL.Optimizer.Pro.Views.TweaksView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1000">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Margin="24,24,24,0">
            <TextBlock Text="TWEAKS" Foreground="White" FontSize="22" FontWeight="Bold"/>
            <TextBlock Text="Ajustes avançados do Windows - cada tweak com descrição, risco e reversibilidade" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,4,0,0"/>
        </StackPanel>

        <Grid Grid.Row="1" Margin="24,16,24,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="300"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal">
                <Button Content="Todos" Style="{StaticResource SecondaryButton}" Margin="0,0,6,0" Command="{Binding SetCategoryCommand}" CommandParameter="Todos"/>
                <Button Content="Windows" Style="{StaticResource SecondaryButton}" Margin="0,0,6,0" Command="{Binding SetCategoryCommand}" CommandParameter="Windows"/>
                <Button Content="Privacidade" Style="{StaticResource SecondaryButton}" Margin="0,0,6,0" Command="{Binding SetCategoryCommand}" CommandParameter="Privacidade"/>
                <Button Content="Interface" Style="{StaticResource SecondaryButton}" Margin="0,0,6,0" Command="{Binding SetCategoryCommand}" CommandParameter="Interface"/>
                <Button Content="Desempenho" Style="{StaticResource SecondaryButton}" Margin="0,0,6,0" Command="{Binding SetCategoryCommand}" CommandParameter="Desempenho"/>
                <Button Content="Rede" Style="{StaticResource SecondaryButton}" Margin="0,0,6,0" Command="{Binding SetCategoryCommand}" CommandParameter="Rede"/>
                <Button Content="Sistema" Style="{StaticResource SecondaryButton}" Margin="0,0,6,0" Command="{Binding SetCategoryCommand}" CommandParameter="Sistema"/>
            </StackPanel>

            <Border Grid.Column="1" Background="{StaticResource SurfaceDarkBrush}" CornerRadius="{StaticResource MediumCorner}" BorderBrush="{StaticResource BorderDarkBrush}" BorderThickness="1" Padding="0,0,12,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="32"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="🔍" Foreground="{StaticResource TextTertiaryBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    <TextBox Grid.Column="1" Background="Transparent" BorderThickness="0" Foreground="White" Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}" Tag="Pesquisar tweaks..." VerticalAlignment="Center" Padding="0,8"/>
                </Grid>
            </Border>
        </Grid>

        <ScrollViewer Grid.Row="2" Margin="24,16,24,24" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding FilteredTweaks}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource CardBorder}" Width="320" Margin="0,0,12,12" Padding="16">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{Binding Name}" Foreground="White" FontWeight="SemiBold" FontSize="12" TextWrapping="Wrap"/>
                                    <Border Grid.Column="1" Background="{StaticResource SurfaceDarkBrush}" CornerRadius="4" Padding="6,2" Margin="8,0,0,0">
                                        <TextBlock Text="{Binding Impact}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="9"/>
                                    </Border>
                                </Grid>

                                <TextBlock Grid.Row="1" Text="{Binding Description}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="10" TextWrapping="Wrap" Margin="0,8,0,0"/>

                                <StackPanel Grid.Row="2" Margin="0,12,0,0">
                                    <Grid Margin="0,0,0,4">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="80"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="Estado atual:" Foreground="{StaticResource TextTertiaryBrush}" FontSize="10"/>
                                        <TextBlock Grid.Column="1" Text="{Binding CurrentState}" Foreground="White" FontSize="10" FontWeight="Medium"/>
                                    </Grid>
                                    <Grid Margin="0,0,0,4">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="80"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="Recomendado:" Foreground="{StaticResource TextTertiaryBrush}" FontSize="10"/>
                                        <TextBlock Grid.Column="1" Text="{Binding RecommendedState}" Foreground="{StaticResource AccentGreenBrush}" FontSize="10" FontWeight="Medium"/>
                                    </Grid>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="80"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="Risco:" Foreground="{StaticResource TextTertiaryBrush}" FontSize="10"/>
                                        <TextBlock Grid.Column="1" Text="{Binding Risk}" Foreground="{StaticResource WarningBrush}" FontSize="10"/>
                                    </Grid>
                                </StackPanel>

                                <Grid Grid.Row="3" Margin="0,16,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Button Content="Aplicar" Style="{StaticResource PrimaryButton}" FontSize="10" Padding="0,6" Margin="0,0,4,0"
                                            Command="{Binding DataContext.ApplyTweakCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}"/>
                                    <Button Grid.Column="1" Content="Reverter" Style="{StaticResource SecondaryButton}" FontSize="10" Padding="0,6" Margin="4,0,0,0"
                                            Command="{Binding DataContext.RevertTweakCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}"/>
                                </Grid>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
