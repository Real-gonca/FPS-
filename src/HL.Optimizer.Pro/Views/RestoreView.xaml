<UserControl x:Class="HL.Optimizer.Pro.Views.RestoreView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1000">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="RESTORE CENTER" Foreground="White" FontSize="22" FontWeight="Bold"/>
            <TextBlock Text="Criar pontos de restauração e backup das configurações alteradas" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,4,0,0"/>
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource CardBorder}" Margin="0,16,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="CRIAR PONTO DE RESTAURAÇÃO" Foreground="{StaticResource TextTertiaryBrush}" FontSize="10" FontWeight="Bold" LetterSpacing="1"/>
                    <TextBox Background="{StaticResource SurfaceDarkBrush}" Foreground="White" BorderBrush="{StaticResource BorderDarkBrush}" Padding="12" Margin="0,12,0,0" Text="{Binding NewRestorePointDescription}"/>
                    <TextBlock Text="Antes de otimizações importantes, recomendamos criar ponto de restauração" Foreground="{StaticResource TextTertiaryBrush}" FontSize="10" Margin="0,8,0,0"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Margin="16,0,0,0" VerticalAlignment="Bottom">
                    <Button Style="{StaticResource SuccessButton}" Content="CRIAR PONTO" Height="40" Padding="20,0" Command="{Binding CreateRestorePointCommand}" IsEnabled="{Binding IsCreating, Converter={StaticResource InverseBoolToVisibilityConverter}}"/>
                    <Button Style="{StaticResource SecondaryButton}" Content="Abrir Restauração do Sistema" Margin="0,8,0,0" Command="{Binding OpenSystemRestoreCommand}"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource CardBorder}" Margin="0,16,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="PONTOS DE RESTAURAÇÃO EXISTENTES" Foreground="{StaticResource TextTertiaryBrush}" FontSize="10" FontWeight="Bold" LetterSpacing="1"/>
                    <Button Grid.Column="1" Style="{StaticResource SecondaryButton}" Content="Atualizar" Command="{Binding RefreshCommand}"/>
                </Grid>

                <ScrollViewer Grid.Row="1" Margin="0,16,0,0" VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding RestorePoints}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="{StaticResource SurfaceDarkBrush}" CornerRadius="{StaticResource MediumCorner}" Padding="16" Margin="0,0,0,8">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <Border Width="40" Height="40" CornerRadius="20" Background="{StaticResource SecondaryDarkBrush}">
                                            <TextBlock Text="♻" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="18"/>
                                        </Border>
                                        <StackPanel Grid.Column="1" Margin="12,0,0,0">
                                            <TextBlock Text="{Binding Description}" Foreground="White" FontSize="12" FontWeight="Medium"/>
                                            <TextBlock Text="{Binding CreationTime, StringFormat={}{0:dd/MM/yyyy HH:mm:ss}}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" Margin="0,4,0,0"/>
                                            <TextBlock Text="{Binding SequenceNumber, StringFormat={}Seq: {0}}" Foreground="{StaticResource TextTertiaryBrush}" FontSize="9" Margin="0,2,0,0"/>
                                        </StackPanel>
                                        <Border Grid.Column="2" Background="{StaticResource CardDarkBrush}" CornerRadius="4" Padding="8,4" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Type}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="9"/>
                                        </Border>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</UserControl>
