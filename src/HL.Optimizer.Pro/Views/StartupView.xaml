<UserControl x:Class="HL.Optimizer.Pro.Views.StartupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1000">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="GERENCIADOR DE INICIALIZAÇÃO" Foreground="White" FontSize="22" FontWeight="Bold"/>
                <TextBlock Text="Gerencie aplicativos de inicialização, serviços e tarefas agendadas - impacto classificado" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,4,0,0"/>
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Border Background="{StaticResource SurfaceDarkBrush}" CornerRadius="{StaticResource MediumCorner}" BorderBrush="{StaticResource BorderDarkBrush}" BorderThickness="1" Padding="0,0,12,0" Margin="0,0,12,0" Width="240">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="32"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="🔍" Foreground="{StaticResource TextTertiaryBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1" Background="Transparent" BorderThickness="0" Foreground="White" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Tag="Pesquisar..." VerticalAlignment="Center" Padding="0,8"/>
                    </Grid>
                </Border>
                <Button Style="{StaticResource PrimaryButton}" Content="Atualizar" Command="{Binding RefreshCommand}"/>
            </StackPanel>
        </Grid>

        <Border Grid.Row="1" Background="{StaticResource SecondaryDarkBrush}" CornerRadius="{StaticResource MediumCorner}" Padding="12" Margin="0,16,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="120"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Nome / Editor" Foreground="{StaticResource TextTertiaryBrush}" FontSize="10" FontWeight="Bold" Margin="48,0,0,0" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="1" Text="Impacto" Foreground="{StaticResource TextTertiaryBrush}" FontSize="10" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="2" Text="Localização" Foreground="{StaticResource TextTertiaryBrush}" FontSize="10" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="3" Text="Estado" Foreground="{StaticResource TextTertiaryBrush}" FontSize="10" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="4" Text="Ações" Foreground="{StaticResource TextTertiaryBrush}" FontSize="10" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <ScrollViewer Grid.Row="2" Margin="0,8,0,0" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding FilteredItems}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{StaticResource CardDarkBrush}" BorderBrush="{StaticResource BorderDarkBrush}" BorderThickness="1" CornerRadius="{StaticResource MediumCorner}" Padding="12" Margin="0,0,0,6">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="120"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Orientation="Horizontal">
                                    <Border Width="36" Height="36" CornerRadius="8" Background="{StaticResource SurfaceDarkBrush}">
                                        <TextBlock Text="🚀" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="14"/>
                                    </Border>
                                    <StackPanel Margin="10,0,0,0" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Name}" Foreground="White" FontSize="11" FontWeight="Medium" MaxWidth="220" TextTrimming="CharacterEllipsis"/>
                                        <TextBlock Text="{Binding Publisher}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="10" MaxWidth="220" TextTrimming="CharacterEllipsis"/>
                                    </StackPanel>
                                </StackPanel>

                                <Border Grid.Column="1" CornerRadius="4" Padding="6,2" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="{StaticResource SurfaceDarkBrush}"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Impact}" Value="Baixo">
                                                    <Setter Property="Background" Value="#0F2A1A"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Impact}" Value="Medio">
                                                    <Setter Property="Background" Value="#4A3A1A"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Impact}" Value="Alto">
                                                    <Setter Property="Background" Value="#4A1A1A"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock Text="{Binding Impact}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="10" HorizontalAlignment="Center"/>
                                </Border>

                                <TextBlock Grid.Column="2" Text="{Binding Location}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="10" HorizontalAlignment="Center" VerticalAlignment="Center"/>

                                <StackPanel Grid.Column="3" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <Border Width="8" Height="8" CornerRadius="4" Background="{StaticResource AccentGreenBrush}" Margin="0,0,6,0" Visibility="{Binding IsEnabled, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                    <Border Width="8" Height="8" CornerRadius="4" Background="{StaticResource TextTertiaryBrush}" Margin="0,0,6,0" Visibility="{Binding IsEnabled, Converter={StaticResource InverseBoolToVisibilityConverter}}"/>
                                    <TextBlock Text="{Binding IsEnabled}" Foreground="White" FontSize="10">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Text" Value="Ativo"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsEnabled}" Value="False">
                                                        <Setter Property="Text" Value="Inativo"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </StackPanel>

                                <StackPanel Grid.Column="4" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <Button Style="{StaticResource SecondaryButton}" Content="Ativar/Desativar" FontSize="9" Padding="8,3" Margin="0,0,4,0"
                                            Command="{Binding DataContext.ToggleItemCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}"/>
                                    <Button Style="{StaticResource IconButton}" Content="📁" ToolTip="Abrir localização" Width="28" Height="28"
                                            Command="{Binding DataContext.OpenLocationCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
