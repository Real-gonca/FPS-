<UserControl x:Class="HL.Optimizer.Pro.Views.CleanupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1000">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="LIMPEZA" Foreground="White" FontSize="22" FontWeight="Bold"/>
            <TextBlock Text="Ferramenta profissional de limpeza - analisa espaço recuperável com segurança" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,4,0,0"/>
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource CardBorder}" Margin="0,16,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Text="ESPAÇO RECUPERÁVEL" Foreground="{StaticResource TextTertiaryBrush}" FontSize="10" FontWeight="Bold" LetterSpacing="1"/>
                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <TextBlock Text="{Binding TotalSizeFormatted}" Foreground="White" FontSize="28" FontWeight="Bold"/>
                        <TextBlock Text="{Binding SelectedCount, StringFormat={}• {0} itens selecionados}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" VerticalAlignment="Center" Margin="12,0,0,0"/>
                    </StackPanel>
                    <TextBlock Text="{Binding CurrentAction}" Foreground="{StaticResource ElectricBlueBrush}" FontSize="11" Margin="0,6,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Style="{StaticResource SecondaryButton}" Content="Selecionar Todos" Margin="0,0,8,0" Command="{Binding SelectAllCommand}"/>
                    <Button Style="{StaticResource SecondaryButton}" Content="Desmarcar Todos" Margin="0,0,12,0" Command="{Binding DeselectAllCommand}"/>
                    <Button Style="{StaticResource PrimaryButton}" Content="ANALISAR" Width="120" Height="40" Command="{Binding AnalyzeCommand}"/>
                </StackPanel>
            </Grid>
        </Border>

        <ScrollViewer Grid.Row="2" Margin="0,16,0,0" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Items}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{StaticResource CardDarkBrush}" BorderBrush="{StaticResource BorderDarkBrush}" BorderThickness="1" CornerRadius="{StaticResource MediumCorner}" Padding="16" Margin="0,0,0,8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <CheckBox IsChecked="{Binding IsSelected}" VerticalAlignment="Center"/>

                                <StackPanel Grid.Column="1" Margin="12,0,0,0">
                                    <TextBlock Text="{Binding Name}" Foreground="White" FontSize="12" FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding Description}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" Margin="0,2,0,0"/>
                                    <TextBlock Text="{Binding Path}" Foreground="{StaticResource TextTertiaryBrush}" FontSize="9" Margin="0,4,0,0" TextTrimming="CharacterEllipsis"/>
                                </StackPanel>

                                <StackPanel Grid.Column="2" Margin="16,0" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding SizeFormatted}" Foreground="White" FontSize="13" FontWeight="Bold" HorizontalAlignment="Right"/>
                                    <TextBlock Text="{Binding FileCount, StringFormat={}{0} arquivos}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="10" HorizontalAlignment="Right"/>
                                </StackPanel>

                                <Border Grid.Column="3" Background="{StaticResource SurfaceDarkBrush}" CornerRadius="4" Padding="8,4" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Category}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="9"/>
                                </Border>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <Border Grid.Row="3" Background="{StaticResource SecondaryDarkBrush}" CornerRadius="{StaticResource LargeCorner}" Padding="16" Margin="0,16,0,0" BorderBrush="{StaticResource BorderDarkBrush}" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="⚠ Aviso de Segurança" Foreground="{StaticResource WarningBrush}" FontSize="11" FontWeight="Bold"/>
                    <TextBlock Text="Nunca apagamos arquivos pessoais automaticamente. Apenas temporários, cache e logs. Lixeira pode ser recuperada. Windows Update cache apenas quando seguro." Foreground="{StaticResource TextSecondaryBrush}" FontSize="10" TextWrapping="Wrap" Margin="0,4,0,0"/>
                </StackPanel>
                <Button Grid.Column="1" Style="{StaticResource SuccessButton}" Content="LIMPAR SELECIONADOS" Height="44" Padding="24,0" FontWeight="Bold" Command="{Binding CleanSelectedCommand}" VerticalAlignment="Center"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
